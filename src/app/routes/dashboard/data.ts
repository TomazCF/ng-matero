import { ApexOptions } from 'apexcharts';

export interface PeriodicElement {
  name: string;
  position: number;
  weight: number;
  symbol: string;
}

export const STATS = [
  {
    title: 'Total Vendido 2',
    amount: '170,200',
    progress: {
      value: 50,
    },
    color: 'bg-azure-50',
  },
  
];
